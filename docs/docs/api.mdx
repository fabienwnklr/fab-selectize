---
title: API Methods
description: api reference
displayed_sidebar: apiSidebar
---

# API Methods

FabSelectize controls can be controlled programmatically via the methods described in this section. When initializing the control, the selectize property is added on the original `<select>` / `<input>` element—this property points to the underlying FabSelectize instance.

```javascript
// initialize the FabSelectize control
var $select = $("select").selectize(options);
```

```javascript
// fetch the instance
var selectize = $select[0].selectize;
```
